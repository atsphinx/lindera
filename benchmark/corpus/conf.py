"""Sphinx configuration for benchmark corpus."""

# -- Project information
project = "benchmark-corpus"

# -- General configuration
exclude_patterns = ["_build"]

# -- Options for HTML output
# html_search_options is overridden programmatically in the benchmark runner

# -- Options for i18n
language = "ja"
